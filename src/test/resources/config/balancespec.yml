---
measures:
    - key: UNIT
      name: { en: " " }
      isInteger: true
types:
    - key: BALANCE
      name: { en: "Balance" }
      measureKey: UNIT
      isAlwaysShow: true
      isWithPockets: true
      removeZeroPockets: true
      entityTypeKey:
        - WALLET
      statuses:
        - key: ACTIVE
          name: { "en": "Active" }
          next:
            - statusKey: LIMITED
              name: { "en": "Limited" }
            - statusKey: BLOCKED
              name: { "en": "Blocked" }
